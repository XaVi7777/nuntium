import 'package:flutter/material.dart';
import 'package:nuntium/core/ui/ui.dart';

class SectionsEmpty extends StatelessWidget {
  const SectionsEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: UILoader(),
    );
  }
}
