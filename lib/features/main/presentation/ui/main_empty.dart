import 'package:flutter/material.dart';

class MainEmpty extends StatelessWidget {
  const MainEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
