import 'package:flutter/material.dart';

class ForgotPasswordEmpty extends StatelessWidget {
  const ForgotPasswordEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
